# Dependencies are declared canonically in pyproject.toml ([project.dependencies]
# and [project.optional-dependencies]). This file installs the package (editable)
# with the viz, api and dev extras so there is a single source of truth and no
# duplicated/drifting version pins.
#
#   pip install -r requirements.txt
#
-e .[viz,api,dev]
